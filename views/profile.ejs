<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10 flex flex-col gap-5">
        <div class="w-full flex justify-end">
            <!-- <h1 class="text-3xl mb-5 font-bold">User Profile</h1>   -->
            <a href="/logout" class="px-3 py-2 text-ms rounded-md bg-red-600 w-20 text-center mb-5 inline-block">Logout</a>
        </div>
        <div class="flex items-start gap-3">
            <div class="w-10 h-9 object-cover "> 
                <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>">  
            </div>
            <h1 class="text-3xl mb-5 ">Hello! Welcome <%= user.name %> ☠️ </h1> 
        </div>
        <h5 class="mb-5">helloo <%= user.name %> You can create a post </h5> 
        <form action="/post" method="post">
            <textarea name="content" placeholder="Write What's in your mind ? " class=" p-3 outline-none w-1/3 resize-none block bg-transparent border-2 border-zinc-800 rounded-md"></textarea>
            <input class="px-3 text-sm w-40 py-2 rounded-md bg-blue-500 block mt-2 " type="submit" value="Create New Post">
        </form>
        <div class="post mt-2">
            <h3 class="text-zinc-400 mb-3">Your Posts.</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 p-4 rounded-md  border-[1px] mb-2 border-zinc-600 bg-zinc-700">
                        <h4 class="mb-4 text-blue-500 font-bold">@<%= user.username %></h4>
                        <p class="text-sm tracking-tighter text-zinc-400"><%= post.content%></p>
                        <small class="inline-block mt-2" ><%= post.likes.length %> likes</small>
                        <div class="mt-1 flex gap-4">
                            <a class="px-3 py-1 bg-green-600 rounded-md text-sm" href="/like/<%= post._id %>" >
                               <%= post.likes.indexOf(user._id) === -1 ? "like" : "unlike" %>
                            </a>
                            <a class="px-3 py-1 bg-red-600 rounded-md text-sm" href="/edit/<%= post._id %>">Edit</a>
                       </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
    <script src="/public/javascripts/script.js"></script>
</body>
</html>